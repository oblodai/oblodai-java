// Generated by oblodai-backend tools/sdkgen from openapi.json - do not edit.

package com.oblodai.generated.models;

import java.math.BigDecimal;
import java.util.LinkedHashMap;
import java.util.LinkedHashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;

/**
 * Build one with {@link #builder()}; fields the SDK does not know yet are kept in {@link #extra()}
 * and sent back as they came.
 */
public final class PayoutCalculation implements WireObject {

    private static final Set<String> FIELDS = Set.of(
            "amount",
            "commission",
            "currency",
            "fee_bearer",
            "fee_type",
            "network",
            "payer_amount");

    private final BigDecimal amount;
    private final BigDecimal commission;
    private final String currency;
    private final PayoutFeeBearer feeBearer;
    private final FeeType feeType;
    private final String network;
    private final BigDecimal payerAmount;
    private final Set<String> nulls;
    private final Map<String, Object> extra;

    private PayoutCalculation(Builder builder) {
        this.amount = builder.amount;
        this.commission = builder.commission;
        this.currency = builder.currency;
        this.feeBearer = builder.feeBearer;
        this.feeType = builder.feeType;
        this.network = builder.network;
        this.payerAmount = builder.payerAmount;
        this.nulls = Set.copyOf(builder.nulls);
        this.extra = Wire.copyMap(builder.extra);
    }

    /**
     * How much will be debited from YOUR balance, in currency (the fee included when you bear it);
     * null — cannot be estimated right now (the fee is unknown and you bear it).
     *
     * @return the {@code amount} field, or {@code null} when absent
     */
    public BigDecimal amount() {
        return amount;
    }

    /**
     * The network fee of the payout, in currency; who bears it is fee_bearer. null — cannot be
     * estimated right now (the fee oracle or the rate is unavailable), not zero: retry later.
     *
     * @return the {@code commission} field, or {@code null} when absent
     */
    public BigDecimal commission() {
        return commission;
    }

    /**
     * Payout asset.
     *
     * @return the {@code currency} field
     */
    public String currency() {
        return currency;
    }

    /**
     * Who pays the network fee: gateway (Oblodai absorbs it, commission is 0), merchant (added to
     * amount, the recipient gets the full sum) or recipient (deducted from payer_amount).
     *
     * @return the {@code fee_bearer} field
     */
    public PayoutFeeBearer feeBearer() {
        return feeBearer;
    }

    /**
     * exact — the fee is contractual (the gateway absorbs it); estimated — an oracle estimate.
     *
     * @return the {@code fee_type} field
     */
    public FeeType feeType() {
        return feeType;
    }

    /**
     * The network — as it came in the request.
     *
     * @return the {@code network} field
     */
    public String network() {
        return network;
    }

    /**
     * How much the RECIPIENT receives at the address, in currency (not what you pay — that is
     * amount). null — cannot be estimated right now (the fee is unknown and the recipient bears
     * it).
     *
     * @return the {@code payer_amount} field, or {@code null} when absent
     */
    public BigDecimal payerAmount() {
        return payerAmount;
    }

    /** @return fields of the answer this SDK version does not know yet, exactly as received */
    public Map<String, Object> extra() {
        return extra;
    }

    /** @return a new, empty builder */
    public static Builder builder() {
        return new Builder();
    }

    /** @return a builder holding this object's values */
    public Builder toBuilder() {
        Builder builder = new Builder();
        builder.amount = this.amount;
        builder.commission = this.commission;
        builder.currency = this.currency;
        builder.feeBearer = this.feeBearer;
        builder.feeType = this.feeType;
        builder.network = this.network;
        builder.payerAmount = this.payerAmount;
        builder.nulls.addAll(this.nulls);
        builder.extra.putAll(this.extra);
        return builder;
    }

    /**
     * @param data the JSON object
     * @return the model
     * @throws IllegalArgumentException when a required field is missing or a value has the wrong type
     */
    public static PayoutCalculation fromMap(Map<String, ?> data) {
        Builder builder = new Builder();
        builder.amount = Wire.optional(data, "amount", Wire::decimal, "PayoutCalculation");
        builder.commission = Wire.optional(data, "commission", Wire::decimal, "PayoutCalculation");
        builder.currency = Wire.required(data, "currency", Wire::string, "PayoutCalculation");
        builder.feeBearer = Wire.required(
                data, "fee_bearer", PayoutFeeBearer::fromJson, "PayoutCalculation");
        builder.feeType = Wire.required(data, "fee_type", FeeType::fromJson, "PayoutCalculation");
        builder.network = Wire.required(data, "network", Wire::string, "PayoutCalculation");
        builder.payerAmount = Wire.optional(
                data, "payer_amount", Wire::decimal, "PayoutCalculation");
        builder.nulls.addAll(Wire.nulls(data, FIELDS));
        builder.extra.putAll(Wire.extra(data, FIELDS));
        return new PayoutCalculation(builder);
    }

    /**
     * @param data a value of the JSON tree
     * @return the model
     * @throws IllegalArgumentException when it is not an object this model can read
     */
    public static PayoutCalculation fromJson(Object data) {
        return fromMap(Wire.object(data, "PayoutCalculation"));
    }

    /** @return the JSON object: set fields, explicit nulls and {@link #extra()} */
    public Map<String, Object> toMap() {
        Map<String, Object> out = new LinkedHashMap<>(this.extra);
        Wire.put(out, "amount", this.amount, this.nulls);
        Wire.put(out, "commission", this.commission, this.nulls);
        Wire.put(out, "currency", this.currency, this.nulls);
        Wire.put(out, "fee_bearer", this.feeBearer, this.nulls);
        Wire.put(out, "fee_type", this.feeType, this.nulls);
        Wire.put(out, "network", this.network, this.nulls);
        Wire.put(out, "payer_amount", this.payerAmount, this.nulls);
        return out;
    }

    @Override
    public Object toJson() {
        return toMap();
    }

    @Override
    public boolean equals(Object other) {
        return other instanceof PayoutCalculation that
                && Objects.equals(this.amount, that.amount)
                && Objects.equals(this.commission, that.commission)
                && Objects.equals(this.currency, that.currency)
                && Objects.equals(this.feeBearer, that.feeBearer)
                && Objects.equals(this.feeType, that.feeType)
                && Objects.equals(this.network, that.network)
                && Objects.equals(this.payerAmount, that.payerAmount)
                && this.nulls.equals(that.nulls)
                && this.extra.equals(that.extra);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
                this.amount,
                this.commission,
                this.currency,
                this.feeBearer,
                this.feeType,
                this.network,
                this.payerAmount,
                this.nulls,
                this.extra);
    }

    @Override
    public String toString() {
        return Wire.describe(
                "PayoutCalculation",
                "amount",
                this.amount,
                "commission",
                this.commission,
                "currency",
                this.currency,
                "feeBearer",
                this.feeBearer,
                "feeType",
                this.feeType,
                "network",
                this.network,
                "payerAmount",
                this.payerAmount,
                "extra",
                this.extra);
    }

    /** Builds a {@link PayoutCalculation}. */
    public static final class Builder {
        private BigDecimal amount;
        private BigDecimal commission;
        private String currency;
        private PayoutFeeBearer feeBearer;
        private FeeType feeType;
        private String network;
        private BigDecimal payerAmount;
        private final Set<String> nulls = new LinkedHashSet<>();
        private final Map<String, Object> extra = new LinkedHashMap<>();

        private Builder() {}

        /**
         * Sets {@code amount}.
         *
         * <p>How much will be debited from YOUR balance, in currency (the fee included when you
         * bear it); null — cannot be estimated right now (the fee is unknown and you bear it).
         *
         * @param amount the value
         * @return this builder
         */
        public Builder amount(BigDecimal amount) {
            this.amount = amount;
            Wire.mark(this.nulls, "amount", amount);
            return this;
        }

        /**
         * Sets {@code amount}.
         *
         * <p>How much will be debited from YOUR balance, in currency (the fee included when you
         * bear it); null — cannot be estimated right now (the fee is unknown and you bear it).
         *
         * @param amount the value as a decimal string, such as {@code "10.50"}
         * @return this builder
         */
        public Builder amount(String amount) {
            return amount(amount == null ? null : new BigDecimal(amount));
        }

        /**
         * Sets {@code commission}.
         *
         * <p>The network fee of the payout, in currency; who bears it is fee_bearer. null — cannot
         * be estimated right now (the fee oracle or the rate is unavailable), not zero: retry
         * later.
         *
         * @param commission the value
         * @return this builder
         */
        public Builder commission(BigDecimal commission) {
            this.commission = commission;
            Wire.mark(this.nulls, "commission", commission);
            return this;
        }

        /**
         * Sets {@code commission}.
         *
         * <p>The network fee of the payout, in currency; who bears it is fee_bearer. null — cannot
         * be estimated right now (the fee oracle or the rate is unavailable), not zero: retry
         * later.
         *
         * @param commission the value as a decimal string, such as {@code "10.50"}
         * @return this builder
         */
        public Builder commission(String commission) {
            return commission(commission == null ? null : new BigDecimal(commission));
        }

        /**
         * Sets {@code currency}.
         *
         * <p>Payout asset.
         *
         * @param currency the value
         * @return this builder
         */
        public Builder currency(String currency) {
            this.currency = currency;
            return this;
        }

        /**
         * Sets {@code fee_bearer}.
         *
         * <p>Who pays the network fee: gateway (Oblodai absorbs it, commission is 0), merchant
         * (added to amount, the recipient gets the full sum) or recipient (deducted from
         * payer_amount).
         *
         * @param feeBearer the value
         * @return this builder
         */
        public Builder feeBearer(PayoutFeeBearer feeBearer) {
            this.feeBearer = feeBearer;
            return this;
        }

        /**
         * Sets {@code fee_bearer}.
         *
         * <p>Who pays the network fee: gateway (Oblodai absorbs it, commission is 0), merchant
         * (added to amount, the recipient gets the full sum) or recipient (deducted from
         * payer_amount).
         *
         * @param feeBearer the value as the API sends it; one this SDK version does not know is
         *     accepted
         * @return this builder
         */
        public Builder feeBearer(String feeBearer) {
            return feeBearer(feeBearer == null ? null : PayoutFeeBearer.of(feeBearer));
        }

        /**
         * Sets {@code fee_type}.
         *
         * <p>exact — the fee is contractual (the gateway absorbs it); estimated — an oracle
         * estimate.
         *
         * @param feeType the value
         * @return this builder
         */
        public Builder feeType(FeeType feeType) {
            this.feeType = feeType;
            return this;
        }

        /**
         * Sets {@code fee_type}.
         *
         * <p>exact — the fee is contractual (the gateway absorbs it); estimated — an oracle
         * estimate.
         *
         * @param feeType the value as the API sends it; one this SDK version does not know is
         *     accepted
         * @return this builder
         */
        public Builder feeType(String feeType) {
            return feeType(feeType == null ? null : FeeType.of(feeType));
        }

        /**
         * Sets {@code network}.
         *
         * <p>The network — as it came in the request.
         *
         * @param network the value
         * @return this builder
         */
        public Builder network(String network) {
            this.network = network;
            return this;
        }

        /**
         * Sets {@code payer_amount}.
         *
         * <p>How much the RECIPIENT receives at the address, in currency (not what you pay — that
         * is amount). null — cannot be estimated right now (the fee is unknown and the recipient
         * bears it).
         *
         * @param payerAmount the value
         * @return this builder
         */
        public Builder payerAmount(BigDecimal payerAmount) {
            this.payerAmount = payerAmount;
            Wire.mark(this.nulls, "payer_amount", payerAmount);
            return this;
        }

        /**
         * Sets {@code payer_amount}.
         *
         * <p>How much the RECIPIENT receives at the address, in currency (not what you pay — that
         * is amount). null — cannot be estimated right now (the fee is unknown and the recipient
         * bears it).
         *
         * @param payerAmount the value as a decimal string, such as {@code "10.50"}
         * @return this builder
         */
        public Builder payerAmount(String payerAmount) {
            return payerAmount(payerAmount == null ? null : new BigDecimal(payerAmount));
        }

        /**
         * A field this SDK version does not know yet, sent as given.
         *
         * @param name the field's JSON name
         * @param value its value: a JSON tree, a model, an enum or a BigDecimal
         * @return this builder
         */
        public Builder putExtra(String name, Object value) {
            this.extra.put(name, value);
            return this;
        }

        /**
         * @return the {@link PayoutCalculation}
         * @throws IllegalStateException when a required field is not set
         */
        public PayoutCalculation build() {
            Wire.require(this.currency, "PayoutCalculation", "currency");
            Wire.require(this.feeBearer, "PayoutCalculation", "fee_bearer");
            Wire.require(this.feeType, "PayoutCalculation", "fee_type");
            Wire.require(this.network, "PayoutCalculation", "network");
            return new PayoutCalculation(this);
        }
    }
}
