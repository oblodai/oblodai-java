// Generated by oblodai-backend tools/sdkgen from openapi.json - do not edit.

package com.oblodai.generated.models;

import java.util.Map;
import java.util.Objects;

/**
 * One of several shapes: {@link #fromJson(Object)} picks the variant whose required fields are all
 * present, the most specific first; an answer no variant matches is kept as {@link Unknown}.
 */
public sealed interface ResolveResult extends WireObject
        permits ResolveRefundResult, ResolveAcceptResult, ResolveResult.Unknown {

    /**
     * @param data a value of the JSON tree
     * @return the matching variant, or {@link Unknown}
     */
    static ResolveResult fromJson(Object data) {
        if (data instanceof Map<?, ?> map) {
            if (Wire.hasAll(map, "address", "amount", "approval_required", "commission", "created_at", "currency", "document_url", "fee_bearer", "is_final", "is_refund", "memo", "network", "order_id", "payer_amount", "payment_order_id", "refund_for", "resolution", "source", "status", "txid", "updated_at", "uuid")) {
                return ResolveRefundResult.fromJson(map);
            }
            if (Wire.hasAll(map, "amount_kept", "currency", "order_id", "payment_uuid", "resolution")) {
                return ResolveAcceptResult.fromJson(map);
            }
        }
        return new Unknown(data);
    }

    /** A value no known variant matches, exactly as received. */
    final class Unknown implements ResolveResult {
        private final Object raw;

        /** @param raw the value as received */
        public Unknown(Object raw) {
            this.raw = raw;
        }

        /** @return the value as received */
        public Object raw() {
            return raw;
        }

        @Override
        public Object toJson() {
            return raw;
        }

        @Override
        public boolean equals(Object other) {
            return other instanceof Unknown that && Objects.equals(raw, that.raw);
        }

        @Override
        public int hashCode() {
            return Objects.hashCode(raw);
        }

        @Override
        public String toString() {
            return Wire.describe("ResolveResult.Unknown", "raw", raw);
        }
    }
}
