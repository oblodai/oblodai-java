// Generated by oblodai-backend tools/sdkgen from openapi.json - do not edit.

package com.oblodai.generated.models;

import java.util.Map;
import java.util.Objects;

/**
 * One of several shapes: {@link #fromJson(Object)} picks the variant whose required fields are all
 * present, the most specific first; an answer no variant matches is kept as {@link Unknown}.
 */
public sealed interface PayoutClaimInfo extends WireObject
        permits PayoutClaimView, PayoutClaimLockedView, PayoutClaimInfo.Unknown {

    /**
     * @param data a value of the JSON tree
     * @return the matching variant, or {@link Unknown}
     */
    static PayoutClaimInfo fromJson(Object data) {
        if (data instanceof Map<?, ?> map) {
            if (Wire.hasAll(map, "amount", "claimable", "commission", "currency", "expires_at", "fee_bearer", "fee_type", "network", "note", "payer_amount", "status", "title")) {
                return PayoutClaimView.fromJson(map);
            }
            if (Wire.hasAll(map, "claimable", "expires_at", "passcode_required", "status", "title")) {
                return PayoutClaimLockedView.fromJson(map);
            }
        }
        return new Unknown(data);
    }

    /** A value no known variant matches, exactly as received. */
    final class Unknown implements PayoutClaimInfo {
        private final Object raw;

        /** @param raw the value as received */
        public Unknown(Object raw) {
            this.raw = raw;
        }

        /** @return the value as received */
        public Object raw() {
            return raw;
        }

        @Override
        public Object toJson() {
            return raw;
        }

        @Override
        public boolean equals(Object other) {
            return other instanceof Unknown that && Objects.equals(raw, that.raw);
        }

        @Override
        public int hashCode() {
            return Objects.hashCode(raw);
        }

        @Override
        public String toString() {
            return Wire.describe("PayoutClaimInfo.Unknown", "raw", raw);
        }
    }
}
