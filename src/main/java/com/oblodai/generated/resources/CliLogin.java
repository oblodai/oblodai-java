// Generated by oblodai-backend tools/sdkgen from openapi.json - do not edit.

package com.oblodai.generated.resources;

import com.oblodai.generated.Routes;
import com.oblodai.generated.models.CLIDeviceAuthorization;
import com.oblodai.generated.models.CLIDeviceRequest;
import com.oblodai.generated.models.CLILogoutResult;
import com.oblodai.generated.models.CLIToken;
import com.oblodai.generated.models.CLITokenRequest;
import com.oblodai.generated.models.Wire;

// --- runtime imports (adjust in tools/sdkgen/java/templates/runtime.java.tmpl) ---
import com.oblodai.RequestOptions;
import com.oblodai.core.Resource;
import com.oblodai.core.Transport;
// --- end of runtime imports ---

/**
 * Browser login of the {@code oblodai} CLI (OAuth 2.0 device authorization, RFC 8628) and logout of
 * its key.
 *
 * <p>The {@code CliLogin} resource: one method per API operation. Every method takes per-call
 * {@code RequestOptions} as its last argument; the overloads without it use the defaults.
 */
public final class CliLogin extends Resource {

    /** @param transport the engine every call goes through */
    public CliLogin(Transport transport) {
        super(transport);
    }

    /**
     * Start a CLI browser login
     *
     * <p>No key: this is how the CLI gets one. Returns {@code device_code} (the CLI's polling
     * secret — never show it), {@code user_code} ({@code ABCD-EFGH}, shown to the user),
     * {@code verification_uri} and {@code verification_uri_complete} (open the latter in the
     * browser), {@code expires_in} (600) and {@code interval} (5). The user signs in to the
     * cabinet, checks the device, picks a store and approves; the key gets that member's team role.
     * At most 10 requests per minute per address ({@code cli.rate_limited}, Retry-After).
     *
     * <p>{@code POST /v1/cli/device} ({@code startCliLogin}).
     *
     * <p>Error codes: {@code cli.bad_name}, {@code cli.rate_limited}, {@code cli.unavailable},
     * {@code internal}, {@code request.bad_json}, {@code request.body_read},
     * {@code request.control_char}, {@code request.duplicate_field}, {@code request.nul_byte},
     * {@code request.overloaded}, {@code request.rate_limited}, {@code request.too_deep}.
     *
     * @param params the request body
     * @param options per-call options ({@code null} for the defaults)
     * @return the result
     */
    public CLIDeviceAuthorization start(CLIDeviceRequest params, RequestOptions options) {
        return call(
                Routes.START_CLI_LOGIN,
                null,
                null,
                Wire.body(params),
                options,
                CLIDeviceAuthorization::fromJson);
    }

    /**
     * Same as {@link #start(CLIDeviceRequest, RequestOptions)} with the omitted arguments
     * {@code null}.
     *
     * @param params the request body
     * @return the result
     */
    public CLIDeviceAuthorization start(CLIDeviceRequest params) {
        return start(params, null);
    }

    /**
     * Same as {@link #start(CLIDeviceRequest, RequestOptions)} with the omitted arguments
     * {@code null}.
     *
     * @param options per-call options ({@code null} for the defaults)
     * @return the result
     */
    public CLIDeviceAuthorization start(RequestOptions options) {
        return start(null, options);
    }

    /**
     * Same as {@link #start(CLIDeviceRequest, RequestOptions)} with the omitted arguments
     * {@code null}.
     *
     * @return the result
     */
    public CLIDeviceAuthorization start() {
        return start(null, null);
    }

    /**
     * Poll a CLI login for its key
     *
     * <p>Poll with {@code device_code} every {@code interval} seconds until it succeeds or fails
     * for good. Errors (400 unless noted): {@code cli.authorization_pending} — keep polling;
     * {@code cli.slow_down} — polled too early, the interval grew by 5 seconds
     * ({@code details.interval}); {@code cli.access_denied} (403) — denied in the browser;
     * {@code cli.expired_token} — start over; {@code cli.invalid_device_code} — unknown, or the key
     * was already handed out. Success returns the CLI key ({@code public_id}, {@code secret},
     * store, {@code mode}, {@code role}, {@code expires_at}) exactly once: the secret is erased on
     * the server as it is handed out, and of two concurrent polls only one gets it.
     *
     * <p>{@code POST /v1/cli/token} ({@code pollCliLogin}).
     *
     * <p>Error codes: {@code cli.access_denied}, {@code cli.authorization_pending},
     * {@code cli.expired_token}, {@code cli.invalid_device_code}, {@code cli.slow_down},
     * {@code cli.unavailable}, {@code internal}, {@code merchant.secret_decrypt},
     * {@code request.bad_json}, {@code request.body_read}, {@code request.control_char},
     * {@code request.duplicate_field}, {@code request.nul_byte}, {@code request.overloaded},
     * {@code request.rate_limited}, {@code request.too_deep}.
     *
     * @param params the request body
     * @param options per-call options ({@code null} for the defaults)
     * @return the result
     */
    public CLIToken poll(CLITokenRequest params, RequestOptions options) {
        return call(
                Routes.POLL_CLI_LOGIN,
                null,
                null,
                Wire.body(params),
                options,
                CLIToken::fromJson);
    }

    /**
     * Same as {@link #poll(CLITokenRequest, RequestOptions)} with the omitted arguments
     * {@code null}.
     *
     * @param params the request body
     * @return the result
     */
    public CLIToken poll(CLITokenRequest params) {
        return poll(params, null);
    }

    /**
     * Log out: revoke this CLI key
     *
     * <p>Revokes the CLI key that signs the request; any role may call it. The integration key gets
     * {@code cli.not_cli_key} (403) — it is rotated in the cabinet, never here.
     *
     * <p>Requires role: Viewer when called with a CLI key.
     *
     * <p>{@code POST /v1/cli/logout} ({@code logoutCli}).
     *
     * <p>Error codes: {@code auth.bad_timestamp}, {@code auth.body_too_large},
     * {@code auth.ip_not_allowed}, {@code cli.not_cli_key}, {@code cli.permission_denied},
     * {@code cli.unavailable}, {@code internal}, {@code merchant.bad_signature},
     * {@code merchant.key_expired}, {@code merchant.key_mode_mismatch},
     * {@code merchant.key_not_found}, {@code merchant.rate_limited},
     * {@code merchant.secret_decrypt}, {@code merchant.suspended}, {@code merchant.unknown_key},
     * {@code request.body_read}, {@code request.control_char}, {@code request.duplicate_field},
     * {@code request.nul_byte}, {@code request.overloaded}, {@code request.rate_limited},
     * {@code request.too_deep}.
     *
     * @param options per-call options ({@code null} for the defaults)
     * @return the result
     */
    public CLILogoutResult logoutCli(RequestOptions options) {
        return call(
                Routes.LOGOUT_CLI,
                null,
                null,
                null,
                options,
                CLILogoutResult::fromJson);
    }

    /**
     * Same as {@link #logoutCli(RequestOptions)} with the omitted arguments {@code null}.
     *
     * @return the result
     */
    public CLILogoutResult logoutCli() {
        return logoutCli(null);
    }
}
